// clone the div only div not content
// get the size with  offsetHeight

//The HTMLElement.offsetHeight read-only property returns the height of an element, including vertical padding and borders, as an integer.

// get the clone div height too
// make clone heigh = to div
// add an event listenere. either in the windows or on the div ON CHANGE to update the sizes
// new clone div applly same css background of container but blur it
